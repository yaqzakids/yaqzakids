name = "yaqza-kids"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Static Vite build — used when deploy command is: npx wrangler deploy
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
